export const PHOTO_MODAL_TEST_IDS = {
  getPhotoModalDialog: (testIdPrefix: string) =>
    `${testIdPrefix}-photo-modal-dialog`,
  getPhotoModalCloseButton: (testIdPrefix: string) =>
    `${testIdPrefix}-photo-modal-close-button`,
  getPhotoModalPhotoModalChildrenWrapper: (testIdPrefix: string) =>
    `${testIdPrefix}-photo-modal-photo-modal-children-wrapper`,
};
