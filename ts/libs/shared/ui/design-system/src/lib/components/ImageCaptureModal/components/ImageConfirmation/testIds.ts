export const IMAGE_CONFIRMATION_TEST_IDS = {
  getImageConfirmationTopText: (testIdPrefix: string) =>
    `${testIdPrefix}-image-confirmation-top-text`,
  getImageConfirmationImage: (testIdPrefix: string) =>
    `${testIdPrefix}-image-confirmation-image`,
  getImageConfirmationImagePlaceholder: (testIdPrefix: string) =>
    `${testIdPrefix}-image-confirmation-image-placeholder`,
  getImageConfirmationRetakeButton: (testIdPrefix: string) =>
    `${testIdPrefix}-image-confirmation-retake-button`,
  getImageConfirmationButton: (testIdPrefix: string) =>
    `${testIdPrefix}-image-confirmation-button`,
};
