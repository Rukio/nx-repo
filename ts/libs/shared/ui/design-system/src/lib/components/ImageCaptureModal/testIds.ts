export const IMAGE_CAPTURE_MODAL_TEST_IDS = {
  getCameraTestId: (testIdPrefix: string) =>
    `${testIdPrefix}-image-capture-modal-camera`,
  getImageConfirmationTestId: (testIdPrefix: string) =>
    `${testIdPrefix}-image-capture-modal-image-confirmation`,
};
