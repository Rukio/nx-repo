import { riskStratificationAdminDataAccess } from './risk-stratification-admin-data-access';

describe('riskStratificationAdminDataAccess', () => {
  it('should work', () => {
    expect(riskStratificationAdminDataAccess()).toEqual(
      'risk-stratification-admin-data-access'
    );
  });
});
