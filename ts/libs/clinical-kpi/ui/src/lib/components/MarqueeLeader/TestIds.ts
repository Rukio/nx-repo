export const MARQUEE_LEADER_TEST_IDS = {
  getBadgeTestId: (rank: number) => `marquee-leader-badge-${rank}`,
  getAvatarTestId: (rank: number) => `marquee-leader-avatar-${rank}`,
  getNameTestId: (rank: number) => `marquee-leader-name-${rank}`,
  getPositionTestId: (rank: number) => `marquee-leader-position-${rank}`,
  getValueTestId: (rank: number) => `marquee-leader-value-${rank}`,
  getValueChangeTestId: (rank: number) => `marquee-leader-value-change-${rank}`,
  getPlaceholderTestId: (rank: number) => `marquee-leader-placeholder-${rank}`,
};
