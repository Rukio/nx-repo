# Dispatch Health Core Nx Plugin

## Generators

### typecheck

Adds a `typecheck` task to a given project.

Example usage: `npx nx g @*company-data-covered*/nx-plugins-core:typecheck <project name>`
