const patientDetailsPrefix = 'patient-details';

export const PATIENT_DETAILS_TEST_IDS = {
  PREFIX: patientDetailsPrefix,
  NAME: `${patientDetailsPrefix}-name`,
  EMAIL: `${patientDetailsPrefix}-email`,
  PHONE_NUMBER: `${patientDetailsPrefix}-phone-number`,
  DOB: `${patientDetailsPrefix}-dob`,
  LEGAL_SEX: `${patientDetailsPrefix}-legal-sex`,
  ASSIGNED_SEX_AT_BIRTH: `${patientDetailsPrefix}-assigned-sex-at-birth`,
  GENDER_IDENTITY: `${patientDetailsPrefix}-gender-identity`,
  REMOVE_PATIENT_BUTTON: `${patientDetailsPrefix}-remove-patient-button`,
};
